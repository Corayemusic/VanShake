import { Icon } from "astro-icon/components";
import ThemeSwitcher from "~/components/theme-switcher.astro";
import type { NavItem, FooterLink } from "~/types";

const navItems: Array<NavItem> = [
  { title: "Servicios", url: "#features" },
  { title: "Workshops", url: "#compatibility" },
  { title: "Producciones", url: "#showcase" },
];

const footerLinks: Array<FooterLink> = [
  {
    url: "https://www.instagram.com/van_shake/",
    description: "Instagram",
    icon: "fa-brands:instagram",
  },
  {
    url: "https://www.tiktok.com/@van_shake",
    description: "TikTok",
    icon: "fa-brands:tiktok",
  },
  {
    url: "https://wa.me/1234567890",
    description: "WhatsApp",
    icon: "fa-brands:whatsapp",
  },
  {
    url: "https://www.linkedin.com/company/van-shake/",
    description: "LinkedIn",
    icon: "fa-brands:linkedin",
  },
];


<header
  id="page-header"
  class="absolute bottom-0 z-20 flex w-full items-center justify-between border-b border-transparent px-8 py-4 text-white"
>
  <a class="flex items-center gap-3 hover:!text-default" href="#">
    <h1 class="sr-only">Astro</h1>
    <Icon name="logomark" class="size-10" />
    <Icon name="wordmark" class="hidden h-4 sm:block" />
  </a>
  <div>
    <div class="flex items-center gap-6">
      <nav class="hidden sm:block">
        <ul class="flex items-center gap-6">
          {
            navItems.map(({ title, url }) => (
              <li>
                <a class="text-sm" href={url}>
                  {title}
                </a>
              </li>
            ))
          }
        </ul>
      </nav>
      <button
        id="open-nav-button"
        type="button"
        class="btn sm:hidden"
        aria-label="Navigation"
      >
        <Icon name="mdi:menu" class="size-8" />
      </button>
      <ThemeSwitcher />
    </div>
    <div id="menu-modal" class="modal hidden" aria-hidden="true">
      <div class="fixed inset-0 bg-default px-8 py-4 text-default">
        <div class="space-y-4" role="dialog" aria-modal="true">
          <header class="text-right">
            <button
              id="close-nav-button"
              type="button"
              class="btn"
              aria-label="Close navigation"
            >
              <Icon name="mdi:close" class="size-8" />
            </button>
          </header>
          <div class="flex justify-center">
            <Icon name="logomark" class="size-16" />
          </div>
          <nav>
            <ul class="flex flex-col">
              {
                navItems.map(({ title, url }) => (
                  <li>
                    <a class="block py-4 text-center text-xl" href={url}>
                      {title}
                    </a>
                  </li>
                ))
              }
            </ul>
          </nav>
        </div>
      </div>
    </div>
  </div>
  <div class="absolute bottom-0 left-0 flex gap-4 p-4">
    {
      socialLinks.map(({ url, icon, description }) => (
        <a href={url} aria-label={description} class="text-white hover:text-gray-400">
          <Icon name={icon} class="size-6" />
        </a>
      ))
    }
  </div>
</header>

