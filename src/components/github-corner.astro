<a
  href="https://github.com/mhyfritz/astro-landing-page"
  aria-label="View source on GitHub"
  class="absolute right-0 top-0 z-10 text-black hover:text-black focus:ring-0 focus:ring-offset-0"
>
  <!-- Triángulo blanco -->
  <svg
    class="fill-white"
    width="100"
    height="100"
    viewBox="0 0 250 250"
    aria-hidden="true"
    style="position: absolute; top: 0; right: 0;"
  >
    <path d="M0,0 L115,115 L130,115 L250,250 L250,0 Z"></path>
  </svg>

  <!-- Sintetizador más grande y reposicionado -->
  <svg
    class="synth-body fill-black"
    width="150"
    height="100"
    viewBox="0 0 250 150"
    aria-hidden="true"
    style="position: absolute; bottom: 10px; right: 10px; transform: rotate(-20deg);"
  >
    <!-- Cuerpo del sintetizador -->
    <rect x="0" y="0" width="150" height="60" fill="black" stroke="white" />

    <!-- Teclas del sintetizador con eventos onclick -->
    <rect class="key" x="10" y="20" width="15" height="20" fill="white" onclick="playSound(0)" />
    <rect class="key" x="30" y="20" width="15" height="20" fill="white" onclick="playSound(1)" />
    <rect class="key" x="50" y="20" width="15" height="20" fill="white" onclick="playSound(2)" />
    <rect class="key" x="70" y="20" width="15" height="20" fill="white" onclick="playSound(3)" />

    <!-- Perillas del sintetizador -->
    <circle class="knob" cx="20" cy="10" r="5" fill="gray" />
    <circle class="knob" cx="40" cy="10" r="5" fill="gray" />
    <circle class="knob" cx="60" cy="10" r="5" fill="gray" />
    <circle class="knob" cx="80" cy="10" r="5" fill="gray" />
  </svg>
</a>

<style>
  a:hover .synth-body {
    animation: synth-wave 560ms ease-in-out;
  }

  @keyframes synth-wave {
    0%,
    100% {
      transform: rotate(-20deg);
    }
    20%,
    60% {
     






